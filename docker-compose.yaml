version: '3'
services:
  app:
    build: .
    command: npm start
    volumes:
      - ./:/app
      - ./node_modules:/node_modules
    environment: 
      - MONGO_CONNECTION: "${MONGO_CONNECTION}"
    ports:
      - 3000:3000