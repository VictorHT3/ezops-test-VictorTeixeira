version: '3'
services:
  app:
    environment: 
      - MONGO_CONNECTION=mongodb+srv://victor:mZQCxSRX1cUqTTAt@cluster0.afhhe.mongodb.net/myFirstDatabase?retryWrites=true&w=majority
    build: .
    command: npm start
    volumes:
      - ./:/app
      - ./node_modules:/node_modules
    ports:
      - 3000:3000