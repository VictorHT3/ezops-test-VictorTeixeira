version: '3'
services:
  app:
    build: .
    command: npm start
    volumes:
      - ./src:/app
      - ./node_modules:/app/node_modules
    ports:
      - 3000:3000
    environment:
      - MONGO_CONNECT=mongodb+srv://victor:mZQCxSRX1cUqTTAt@cluster0.afhhe.mongodb.net/myFirstDatabase?retryWrites=true&w=majority